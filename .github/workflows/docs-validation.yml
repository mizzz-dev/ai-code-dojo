name: docs-validation
on:
  pull_request:
    paths:
      - 'docs/**'
      - '.github/**'
      - 'scripts/**'
jobs:
  validate-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/validate-ai-protocol-docs.sh
