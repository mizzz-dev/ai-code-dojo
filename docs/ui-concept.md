# UI コンセプト（学習者向け）

## 目的
学習者が「次に何をすべきか」を迷わないUIを最優先にする。

## トーン
- 学習者向け: 明るく、安心感、CTA強調
- 管理画面向け: 今回は未実装（将来分離）

## 方針
- コンポーネント再利用を前提に `packages/ui` へ集約
- 表示状態（success/fail/warning）をトークンで統一
- モバイルは復習・結果確認導線を重視
